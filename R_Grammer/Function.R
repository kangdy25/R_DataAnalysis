x <- c(1, 2, 3)
x

mean(x)
max(x)
min(x)

str = c("Hello", "World!", "is", "good!")
str

paste(str, collapse = ",") # 쉼표를 구분자로 str의 단어들 하나로 합치기
paste(str, collapse = " ") 

x_mean <- mean(x)
x_mean

str_paste <- paste(str, collapse = " ")
str_paste